"""
`Query` Views module. Views permit the addition,
modification, deletion, fetching, and processing of data
through the Request/Response cycle. `Query` provides insights
into users' behavior, particularly in regards to committed
searches.
"""
