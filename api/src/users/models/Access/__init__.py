"""
`Access` module. `Access` represents a fundamental
component of our role-based access control. It confers
a role to a user, granting that user special permissions &
access based on that role.
"""

from users.models.Access.Access import Access
