"""
Revoke update test module. These tests validate
the functionality of controllers that update `Access` data
in the database. These tests are a subset of
update tests that specifically test the ability to
revoke a user's access.
"""
