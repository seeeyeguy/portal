"""
`ProgramRole` module. `ProgramRole` represents the
specific role that is assigned to a `ProgramMember` on
a `Program`. A `ProgramRole`, at present, consists of either a
Program Manager (PGM) or a Program Financial Analyst (PFA)
which identifies the specific role a `ProgramMember` may have
on a specific `Program`.
"""

from .ProgramRole import ProgramRole
