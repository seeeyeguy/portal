"""
`Disposition` Controllers module. Controllers permit the addition,
modification, deletion, fetching, and processing of data.
`Disposition` represents a vote by a BI Portal admin on a request to
add/modify a directory resource.
"""

from .Disposition import Disposition
