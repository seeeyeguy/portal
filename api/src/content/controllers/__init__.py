"""
`Content` controllers module. Controllers permit the addition,
modification, deletion, fetching, and processing of data. `Content`
manages dynamic content in `BI Portal`.
"""

from .Content import Content
