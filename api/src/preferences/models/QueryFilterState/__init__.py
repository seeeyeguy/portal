"""
`QueryFilterState` module. `QueryFilterState` represents a set
of preferred search terms & filters that may be loaded into the UI,
to improve the user's experience. When a user saves a `QueryFilterState`,
it can be used it to render the UI with a preferred view of resources.
"""

from preferences.models.QueryFilterState.QueryFilterState import QueryFilterState
