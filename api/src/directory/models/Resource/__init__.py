"""
`Resource` module. `Resource` represents a set of links
to internal applications, data services, files, etc that are offered
by various departments within L3Harris Technologies. `Resource`s
provides the primary content for the BI Portal, allowing it to
act as a directory for L3Harris's internal tools. These `Resource`s
will be displayed as UI components on BI Portal's web app, allowing
users to browse, search, filter, and navigate to these internal tools.
"""

from directory.models.Resource.Resource import Resource
