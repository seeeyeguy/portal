"""
`Resource` Controllers Test module. These tests
are specifically designed for the `functree` structure
of resource search. `Functree` structures the `Resource`s
as a dictionary with `Function`-`SubFunction`-`Resource`
hierarchy, where the structure is a dictionary where primary
level keys are `Function` names, secondary level keys are
`SubFunction` names and the values are a collection of 
`Resource`s.
"""
