import ProgramReviewTool from "state/slices/ProgramReviewToolSlice";
import ResourceSearch from "state/slices/ResourceSearchActions";

const AppReducers = {
  ResourceSearch,
  ProgramReviewTool,
};

export default AppReducers;
