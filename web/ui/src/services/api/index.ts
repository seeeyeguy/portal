import baseEndpoints from "services/api/baseEndpoints";
import portalEndpoints from "services/api/portalEndpoints";

const endpoints = {
  SERVICE: baseEndpoints,
  PORTAL: portalEndpoints,
};

export default endpoints;
