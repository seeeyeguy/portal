services:  
  swagger:
      image: swaggerapi/swagger-ui
      ports:
        - ${SWAGGER_PORT}:8080
      volumes:
        - ./init/swagger.yml:/swagger/swagger.yml
      environment:
        - SWAGGER_JSON=/swagger/swagger.yml